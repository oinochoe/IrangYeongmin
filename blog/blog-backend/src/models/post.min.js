const mongoose=require("mongoose"),{Schema:Schema}=mongoose,Post=new Schema({title:String,body:String,tags:[String],publishedDate:{type:Date,default:new Date}});module.exports=mongoose.model("Post",Post);